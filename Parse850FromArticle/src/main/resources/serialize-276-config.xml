<?xml version="1.0"?>
<smooks-resource-list
        xmlns="https://www.smooks.org/xsd/smooks-2.0.xsd"
        xmlns:edi="https://www.smooks.org/xsd/smooks/edi-2.0.xsd">

    <!-- Serialize XML (conforming to 835_mapping.dfdl.xsd) back to X12 835 EDI -->
    <edi:unparser unparseOnNode="*"
                  schemaUri="276_mapping.dfdl.xsd"
                  segmentTerminator="~"
                  dataElementSeparator="*"
                  validationMode="Full">
    </edi:unparser>
</smooks-resource-list>